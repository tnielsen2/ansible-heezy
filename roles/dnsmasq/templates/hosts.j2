# Auto-generated DNS hosts file from Ansible inventory
{% for host in groups['all'] %}
{% if hostvars[host]['ansible_default_ipv4'] is defined %}
{{ hostvars[host]['ansible_default_ipv4']['address'] }} {{ hostvars[host]['hostname'] | default(host) }}.{{ dns_domain | default('heezy.local') }} {{ hostvars[host]['hostname'] | default(host) }}
{% endif %}
{% endfor %}